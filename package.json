{
  "name": "forkify",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -r .parcel-cache && parcel index.html",
    "build": "rm -r .parcel-cache && parcel build index.html"
  },
  "keywords": [],
  "author": "Yousef Nasr",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@parcel/transformer-sass": "2.12.0"
  }
}
